# @package _global_
# Example experiment: MNIST with ResNet18

defaults:
  - override /model: default
  - override /dataset: mnist
  - override /training: default

# Experiment name for logging
experiment_name: mnist_resnet18

# Model overrides
model:
  model_name: resnet18
  pretrained: true
  num_classes: 10

# Dataset overrides
dataset:
  data_dir: ./data
  image_size: 224

# Transformation overrides (MNIST preset already disables these, but keeping for clarity)
transformation:
  train:
    random_crop: false
    horizontal_flip: false

# Training overrides
training:
  epochs: 10
  batch_size: 64
  optimizer:
    lr: 1e-3
    weight_decay: 0.01
  scheduler:
    name: cosine
    warmup_epochs: 1
