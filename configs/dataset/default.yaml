# Default dataset configuration (MNIST)
# For better organization, use dataset-specific configs:
#   python train.py dataset=mnist (this default)
#   python train.py dataset=cifar10
#   python train.py dataset=cifar100
#   python train.py dataset=imagenet
#   python train.py dataset=custom

defaults:
  - mnist
